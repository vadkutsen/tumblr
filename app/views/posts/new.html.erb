<div id="page_wrapper">
	<h1>Make Something</h1>

	<%= form_for :post, url: posts_path do |f| %>
		<p>
			<%= f.label :title %><br>
			<%= f.text_field :title %>
		</p>

		<P>
			<%= f.label :body %><br>
			<%= f.text_area :body %>
		</P>

		<p>
			<%= f.submit %>
		</p>

	<% end %>
</div>